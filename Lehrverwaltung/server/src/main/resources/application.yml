spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/lehrverwaltung
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
server:
  servlet:
    session:
      timeout: 420m
logging:
  level:
    tie:
      lehrlinge:
        lehrverwaltung:
          server:
            service:
              DEBUG
            util:
              DEBUG
docker-java:
  image:
    name: ghcr.io/graalvm/graalvm-ce:latest
  host:
    url: tcp://localhost:2375
  container:
    name: bladerunner
